import { useDispatch, useSelector } from "react-redux";

import { getData } from "../redux/seasonal/seasonSlice";

export default function Home() {
  const dispatch = useDispatch();

  return <div>Sectional</div>;
}
